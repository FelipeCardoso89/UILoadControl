# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
 #cache: cocoapods
 #podfile: Example/Podfile
 #before_install:
 #- gem install cocoapods # Since Travis is not always on latest version
 #- pod install --project-directory=Example

#script:
#- set -o pipefail && xcodebuild test -workspace Example/UILoadControl.xcworkspace -scheme UILoadControl-Example -sdk iphonesimulator #ONLY_ACTIVE_ARCH=NO | xcpretty

script:  
set -e && xcodebuild -workspace Example/UILoadControl.xcworkspace -scheme UILoadControl-Example -sdk iphonesimulator9.0 test | xcpretty -c

- pod lib lint
